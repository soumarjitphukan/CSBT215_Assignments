# Assignment 18

## Question

Data utility class that reads an integer from a file, multiplies by 10, handles errors. Python: ScoreProcessor.process_score_file(), try-except-else-finally, pytest suite. Catch FileNotFoundError and ValueError.
