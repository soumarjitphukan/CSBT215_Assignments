# Assignment 14

## Question

Create a scenario in Python where objects are dead but reference count > 0 (reference cycle). Use Node class, sys.getrefcount(), gc module, gc.collect(). Demonstrate cycle creation, deletion, and cleanup.
